# Flask Backend Dependencies
# Install with: pip install -r requirements.txt

flask>=2.3.0
flask-cors>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
PyJWT>=2.8.0
gunicorn>=21.0.0

# Optional: For ML model persistence
# pickle is built-in, but joblib is better for sklearn models
joblib>=1.3.0

# Optional: For production deployment
# python-dotenv>=1.0.0

